const counter = (array) => {
    return `There are ${array.length} elements in the array`;
};

const multiply = (x, y) => {
    return `${x} times ${y} equle to ${x * y}`;
}

const some_value = 451

module.exports = {
    counter,
    multiply,
    some_value
};
